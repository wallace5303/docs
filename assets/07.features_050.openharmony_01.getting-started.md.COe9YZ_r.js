import{_ as s,o as i,c as a,a3 as t}from"./chunks/framework.BveOjhN0.js";const k=JSON.parse('{"title":"OpenHarmony","description":"","frontmatter":{"title":"OpenHarmony"},"headers":[],"relativePath":"07.features/050.openharmony/01.getting-started.md","filePath":"07.features/050.openharmony/01.getting-started.md","lastUpdated":1782620269000}'),n={name:"07.features/050.openharmony/01.getting-started.md"};function o(r,e,d,l,p,h){return i(),a("div",null,[...e[0]||(e[0]=[t(`<h2 id="introduction" tabindex="-1">Introduction <a class="header-anchor" href="#introduction" aria-label="Permalink to &quot;Introduction&quot;">​</a></h2><p>electron-egg supports packaging your application for HarmonyOS (OpenHarmony). The Electron build artifacts are extracted and copied into a HarmonyOS HAP resource directory, where the HAP project&#39;s web engine loads them.</p><p>This is handled by the <code>ohos</code> command in <code>ee-bin</code>, which follows electron-builder&#39;s <code>extraResources</code> FileSet pattern (<code>from</code> / <code>to</code> / <code>filter</code>) for flexible resource selection.</p><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h2><p>Declare the resources to extract under the <code>ohos</code> key in your <code>bin.js</code> config. Each entry is a FileSet:</p><div class="language-javascript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// config/bin.js (or bin.json)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">module</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">exports</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // ...other ee-bin config</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ohos: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    resources: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        from: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;out/extraResources&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        to: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;ohos_hap/web_engine/resources&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        filter: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;**/*&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;!compiled&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// copy everything except the compiled dir</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>from</code></td><td><code>string</code></td><td>Source path (relative to project root) of the build artifacts to copy</td></tr><tr><td><code>to</code></td><td><code>string</code></td><td>Destination path (relative to project root) inside the HAP resource directory</td></tr><tr><td><code>filter</code></td><td><code>string[]</code></td><td>Glob patterns. <code>**/*</code> matches all; <code>!pattern</code> excludes. Defaults to <code>[&#39;**/*&#39;]</code></td></tr></tbody></table><h2 id="usage" tabindex="-1">Usage <a class="header-anchor" href="#usage" aria-label="Permalink to &quot;Usage&quot;">​</a></h2><p>Run the <code>ohos-resources</code> npm script (defined in <code>package.json</code>):</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ohos-resources</span></span></code></pre></div><p>Which executes:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ee-bin</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ohos</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --cmds=resources</span></span></code></pre></div><p><code>--cmds</code> accepts a comma-separated list of keys under <code>ohos</code> to process. If omitted, all array-type properties under <code>ohos</code> are processed.</p><h2 id="how-it-works" tabindex="-1">How it works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to &quot;How it works&quot;">​</a></h2><p>For each resource entry, <code>ee-bin</code>:</p><ol><li>Validates that the <code>from</code> source path exists (skips with a warning if not).</li><li>Selects files matching the <code>filter</code> patterns via glob (supports negation like <code>!compiled</code>).</li><li>Copies each matched file to <code>to</code>, preserving directory structure.</li><li>Uses an <strong>atomic copy strategy</strong>: the existing destination is renamed to a <code>.bak</code> backup before copying, and the backup is deleted only after the new copy succeeds. If the copy fails, the backup is restored — so a failed run never leaves a broken destination.</li></ol>`,16)])])}const g=s(n,[["render",o]]);export{k as __pageData,g as default};
